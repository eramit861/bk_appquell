/**
 * Tab 5 - Step 2: Spouse Expenses Separate Household
 * Expenses for spouse if living separately
 */

// All functionality handled by common.js
// This step uses the same expense calculation functions as step 1

// Export for consistency
window.initializeStep2 = function() {
    // Step 2 uses common functions
};

